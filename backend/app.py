# Import the main DjangoManager class from the main script
from main import DjangoManager

# Entry point: run the manager's main method when the script is executed
if __name__ == '__main__':
    DjangoManager().app()
